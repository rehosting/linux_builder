name: Compile and release

on:
  push:
    tags:
      - 'v*'

  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  prebuild:
    runs-on: rehosting-arc
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}
      
      - name: Trust Harbor's self-signed certificate
        run: |
          echo "Fetching certificate from ${{ secrets.REHOSTING_ARC_REGISTRY }}"
          openssl s_client -showcerts -connect ${{ secrets.REHOSTING_ARC_REGISTRY }}:443 < /dev/null 2>/dev/null | openssl x509 -outform PEM | sudo tee /usr/local/share/ca-certificates/harbor.crt > /dev/null
          sudo update-ca-certificates
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:master
            network=host
          buildkitd-config-inline: |
            [registry."${{ secrets.REHOSTING_ARC_REGISTRY }}"]
              insecure = true
              http = true

      - name: Log git revisions of all linux projects
        run: |
          echo "Main repo revision:" && git rev-parse HEAD
          echo
          echo "Submodule revisions:" && git submodule status
          echo
          echo "Full submodule SHAs:" && git submodule foreach 'echo $name: $(git rev-parse HEAD)'

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: rehosting
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Log in to Rehosting Arc Registry
        uses: docker/login-action@v3
        with:
          registry: ${{secrets.REHOSTING_ARC_REGISTRY}}
          username: ${{ secrets.REHOSTING_ARC_REGISTRY_USER }}
          password: ${{ secrets.REHOSTING_ARC_REGISTRY_PASSWORD }}

      # Build this first, build.sh will use it later
      - name: Build kernel_builder docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:${{ github.ref_name }}
            ${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:latest
          build-args: |
            REGISTRY=${{ secrets.REHOSTING_ARC_REGISTRY }}/proxy
          cache-from: |
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:cache,mode=max
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builderr:cache-PR-${{github.event.number}},mode=max
          cache-to: |
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:cache,mode=max
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:cache-PR-${{ github.event.number }}

      - name: Create ephemeral Dockerfile
        run: |
          cat <<EOF > Dockerfile.linux_builder
          FROM ${{secrets.REHOSTING_ARC_REGISTRY}}/pandare/kernel_builder:${{ github.ref_name }}
          COPY . /linux_builder
          RUN ln -s /linux_builder /app
          WORKDIR /app
          EOF

      - name: Update submodules
        run: git submodule update --init --depth 1
      
      # no longer pushing to dockerhub since harbor replicates for us
      - name: Build Docker image and push to harbor
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.linux_builder
          push: true
          tags: |
            ${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/linux_builder:${{ github.ref_name }}
            ${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/linux_builder:latest
          build-args: |
            REGISTRY=${{ secrets.REHOSTING_ARC_REGISTRY }}/proxy
          cache-from: |
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/linux_builder:cache,mode=max
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/linux_builder:cache-PR-${{github.event.number}},mode=max
          cache-to: |
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/penguin:cache,mode=max
            type=registry,ref=${{secrets.REHOSTING_ARC_REGISTRY}}/rehosting/penguin:cache-PR-${{ github.event.number }}

  build:
    needs: prebuild
    # Only publish on tags. run git tag vX and git push origin vX
    # runs-on: self-hosted
    runs-on: rehosting-arc
    if: github.event.pull_request.draft == false

    strategy:
      matrix:
        target: [armel, arm64, mipsel, mipseb, mips64el, mips64eb, powerpc, powerpc64, loongarch64, riscv64, x86_64]
        version: ["6.13"] # XXX: quotes are necessary, otherwise 4.10 -> 4.1

    steps:
      - name: Trust Harbor's self-signed certificate
        run: |
          echo "Fetching certificate from ${{ secrets.REHOSTING_ARC_REGISTRY }}"
          openssl s_client -showcerts -connect ${{ secrets.REHOSTING_ARC_REGISTRY }}:443 < /dev/null 2>/dev/null | openssl x509 -outform PEM | sudo tee /usr/local/share/ca-certificates/harbor.crt > /dev/null
          sudo update-ca-certificates

      - name: Log in to Rehosting Arc Registry
        uses: docker/login-action@v3
        with:
          registry: ${{secrets.REHOSTING_ARC_REGISTRY}}
          username: ${{ secrets.REHOSTING_ARC_REGISTRY_USER }}
          password: ${{ secrets.REHOSTING_ARC_REGISTRY_PASSWORD }}

      - name: Pull prebuilt image
        run: |
          docker pull ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder:${{ github.ref_name }}
          docker tag ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder:${{ github.ref_name }} rehosting/linux_builder:${{ github.ref_name }}

      - uses: oras-project/setup-oras@v1.2.3

      - name: Restore kernel cache from registry (if exists)
        run: |
          set -e
          if oras manifest fetch ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder_cache:latest > /dev/null 2>&1; then
            oras pull ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder_cache:latest -o kernel-cache.tar.gz
            tar -xzf kernel-cache.tar.gz
          else
            echo "No cache found in registry."
          fi

      - name: Build Kernel for ${{ matrix.target }}
        run: |
            mkdir -p cache
            docker run --rm -v $PWD/cache:/tmp/build \
              rehosting/linux_builder:${{ github.ref_name }} \
              bash -c "./_in_container_build.sh false ${{ matrix.version }} ${{ matrix.target}} false false false true && cp kernels-latest.tar.gz /tmp/build/kernels-latest.tar.gz && cp /app/kernel-devel-all.tar.gz /tmp/build/kernel-devel-all.tar.gz"
            mv cache/kernels-latest.tar.gz kernels-latest.tar.gz
            mv cache/kernel-devel-all.tar.gz kernel-devel-all.tar.gz
            tar -czvf build-output-${{ matrix.target }}.${{ matrix.version }}.tar.gz kernels-latest.tar.gz kernel-devel-all.tar.gz

      - name: Save kernel cache to registry
        run: |
          tar -czvf kernel-cache.tar.gz cache || true
          oras push ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder_cache:latest kernel-cache.tar.gz:application/gzip

      - name: Push build artifact to registry with oras
        run: |
          oras push ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder_artifact:${{ matrix.target }}-${{ matrix.version }} build-output-${{ matrix.target }}.${{ matrix.version }}.tar.gz:application/gzip

      - name: Upload build revision log
        uses: actions/upload-artifact@v4
        with:
          name: build-revisions
          path: build-revisions.txt

  aggregate:
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: rehosting-arc
    env:
      MATRIX_VERSIONS: ${{ toJSON(needs.build.strategy.matrix.version) }}
    permissions:
      actions: write
      contents: write
    steps:
      - uses: oras-project/setup-oras@v1.2.3

      - name: Pull all build artifacts from registry with oras
        run: |
          mkdir -p downloaded-kernels
          for target in armel arm64 mipsel mipseb mips64el mips64eb powerpc powerpc64 loongarch64 riscv64 x86_64; do
            for version in 6.13; do
              tag="$target-$version"
              oras pull ${{ secrets.REHOSTING_ARC_REGISTRY }}/rehosting/linux_builder_artifact:$tag -o downloaded-kernels/build-output-$tag.tar.gz || echo "No artifact for $tag"
            done
          done

      - name: Extract all build outputs
        run: |
          rm -rf extracted-kernels && mkdir extracted-kernels
          for archive in $(find downloaded-kernels -name "build-output-*.tar.gz"); do
            tar -xzf "$archive" -C extracted-kernels
          done

      - name: Combine all kernels into a single archive
        run: |
          rm -rf combined-kernels && mkdir combined-kernels
          # Only extract kernels-latest.tar.gz archives, not intermediate ones
          for archive in $(find extracted-kernels -name "kernels-latest.tar.gz"); do
            tar -xzf "$archive" -C combined-kernels
          done

          # Combine OSI profiles for each kernel version
          # otherwise the extracted kernel configs will clobber each other
          if [ -d combined-kernels/kernels/4.10 ] ; then
            for archive in $(find extracted-kernels -name "kernels-latest.tar.gz"); do
              tar -O -xf "$archive" "kernels/4.10/osi.config";
            done > combined-kernels/kernels/4.10/osi.config
          fi

          if [ -d combined-kernels/kernels/6.13 ] ; then
            for archive in $(find extracted-kernels -name "kernels-latest.tar.gz"); do
              tar -O -xf "$archive" "kernels/6.13/osi.config";
            done > combined-kernels/kernels/6.13/osi.config
          fi

          # Create a new single archive from the combined content
          tar -czvf kernels-latest.tar.gz -C combined-kernels .

      - name: Aggregate all kernel-devel artifacts
        run: |
          mkdir -p kernel-devel-all
          find extracted-kernels -name "kernel-devel-all.tar.gz" -exec cp {} kernel-devel-all/ \;
          tar -czvf kernel-devel-all.tar.gz -C kernel-devel-all .

      - name: Create and publish release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            kernels-latest.tar.gz
            kernel-devel-all.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref_name }}
